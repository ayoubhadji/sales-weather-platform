# Project Analysis — Initial Findings

Date: 2026-08-05

## Summary so far

- Backend (`backend/api`): NestJS + TypeORM. Authentication via JWT; `User` entity has a `role` (`ADMIN` | `FRANCHISE`) and `SalesTicket` links to `User` via `user` (franchise owner). `SalesTicketService` enforces franchise-scoped queries in `findAllForUser` and `findOneForUser`.
- Frontend (`frontend`): React + TypeScript + Vite. `AuthContext` and `authService` persist user and token in `localStorage`; `user.role` and `user.id` are stored. Ticket creation flow exists (`NewTicket`) and `TicketContext` manages cart state.
- Authentication: `AuthService` signs JWT with `sub`, `email`, `role`. `JwtStrategy` validates and attaches `id`, `email`, `role` to `req.user`.

## Immediate notes / security

- Ownership model: Franchise is a `User` with role `FRANCHISE`. Backend enforces franchise ticket visibility at query level (recommended pattern).
- Ensure frontend uses authenticated endpoints and doesn't assume client-side filtering only.

## Next suggested steps

1. Run a deeper automated scan of all source files to produce a per-file summary (API surface, responsibilities, potential issues).
2. Verify CI/CD and deployment configs (Azure, Vercel, Neon) for secrets and env var handling.
3. Audit backend tests and lint findings and prioritize fixes.
4. Produce a shortlist of high-priority items (security, transactional ticket creation, production-ready JWT secret management).

---

_This file was auto-generated by the analysis assistant. If you want a full per-file report, say `Proceed` and I'll generate a detailed report (one line per file) and save it to `docs/project-analysis-full.md`."